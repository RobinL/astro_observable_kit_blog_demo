---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Notebook Demo">
    <Fragment slot="head">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@observablehq/inspector@5/dist/inspector.css" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@observablehq/inputs@0.12.0/dist/inputs.css" />
        <style>
            .grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 20px;
                margin: 20px 0;
            }
            .card {
                padding: 20px;
                border: 1px solid #ccc;
                border-radius: 8px;
                background: white;
            }
            h1 {
                color: #333;
                border-bottom: 2px solid #0066cc;
                padding-bottom: 10px;
            }
            h3 {
                margin-top: 0;
                color: #666;
            }
            .demo-nav {
                margin: 20px 0;
                padding: 15px;
                background: #f0f7ff;
                border-radius: 8px;
                border: 1px solid #cce3ff;
            }
            .demo-nav ul {
                margin: 10px 0 0 20px;
                padding: 0;
            }
            .demo-nav li {
                margin: 5px 0;
            }
        </style>
    </Fragment>

    <h1>Observable Notebook Demos</h1>

    <div class="demo-nav">
        <strong>Explore other demo pages:</strong>
        <ul>
            <li><a href={`${base}/demo-notebook`}>Full Embedded Notebook</a> - Renders an entire Observable notebook in one container.</li>
            <li><a href={`${base}/demo-cells`}>Individual Notebook Cells</a> - Embeds specific cells from a notebook directly into MDX content.</li>
        </ul>
    </div>

    <hr />

    <h2>Inline Cell Example</h2>
    <p>Below is a quick demonstration of individual cells being mounted directly on this page.</p>

    <div id="cell-1"></div>

    <div class="grid">
        <div class="card">
            <h3>Input 1</h3>
            <div data-cell="one"></div>
        </div>
        <div class="card">
            <h3>Input 2</h3>
            <div data-cell="other"></div>
        </div>
    </div>

    <div class="card">
        <h3>Result</h3>
        <div data-cell="hi_there"></div>
    </div>

    <!-- Any unmatched cells will be appended here in document order -->
    <div id="notebook"></div>

    <script>
        // Astro will bundle this with Vite, resolving the workspace import
        import { mount } from 'hello-world-diff-demo';

        let currentRuntime;

        function mountNotebook() {
            try {
                // Dispose any previous runtime (HMR / remount safety)
                if (currentRuntime) currentRuntime.dispose();

                const container = document.getElementById('notebook') ?? document.body;
                const { runtime } = mount(container);
                currentRuntime = runtime;
            } catch (err) {
                console.error('Error mounting notebook:', err);
            }
        }

        // Wait for DOM to be ready
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', mountNotebook);
        } else {
            mountNotebook();
        }

        // HMR cleanup
        if (import.meta.hot) {
            import.meta.hot.dispose(() => {
                if (currentRuntime) currentRuntime.dispose();
            });
        }
    </script>
</BaseLayout>
